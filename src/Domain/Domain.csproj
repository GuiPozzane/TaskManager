<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
	<RootNamespace>TaskManager.$(MSBuildProjectName.Replace(" ", "_"))</RootNamespace>
	<AssemblyName>TaskManager.$(MSBuildProjectName)</AssemblyName>  
  </PropertyGroup>

</Project>
